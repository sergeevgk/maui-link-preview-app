<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:LinkPreviewApp"
			 x:Class="LinkPreviewApp.MainPage"
			 Title="LinkPreviewApp">

	<ScrollView>
		<VerticalStackLayout
			Padding="30,0"
			Spacing="25">

			<Label 
				x:Name="urlPreviewText"
				Text="Here go the link previews"
				Style="{StaticResource SubHeadline}"
				SemanticProperties.HeadingLevel="Level1"
				SemanticProperties.Description="Headline"
				VerticalOptions="EndAndExpand"
				/>

			<CollectionView 
				ItemsSource="{Binding LinkPreviews}" >
				<CollectionView.ItemTemplate>
					<DataTemplate>
						<local:LinkPreviewItemTemplate />
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>

			<Label
				Text="Here goes the URL preview text"
				SemanticProperties.Description="URL previewer"
				/>

			<Editor 
				x:Name="urlInputEditor"
				AutoSize="Disabled"
				HeightRequest="100"
				HorizontalTextAlignment="Start"
				VerticalTextAlignment="Start"
				IsSpellCheckEnabled="False"
				Keyboard="Url"
				MaxLength="500"
				Placeholder="Input your URL here"
				Text="{Binding EnteredUrl}" 
				/>

			<Button 
				x:Name="previewLinkButton"
				HeightRequest="30"
				Text="Preview Link"
				Command="{ Binding FetchPreviewCommand }"
				/>
		</VerticalStackLayout>
	</ScrollView>

</ContentPage>
